[project]
name = "finsight"
version = "0.1.0"
description = "Personal finance RAG assistant — multi-institution ingestion, normalization, and natural-language querying"
requires-python = ">=3.11"

dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.2",
    "pgvector>=0.3",
    "alembic>=1.14",
    "pandas>=2.2",
    "pdfplumber>=0.11",
    "camelot-py>=0.11",
    "openpyxl>=3.1",
    "scikit-learn>=1.5",
    "sentence-transformers>=3.3",
    "anthropic>=0.40",
    "apscheduler>=3.10",
    "python-multipart>=0.0.12",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "httpx>=0.27",
    "ruff>=0.8",
]

[tool.setuptools.packages.find]
include = ["app*"]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
